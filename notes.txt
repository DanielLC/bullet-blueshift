Doing:

Improving errors and having it show a stack trace for problems in my C#, but not for problems in the user's script.

TODO:

Necessary for pre-alpha:

Make it so you can set the sprite and size of spawned objects, and change their position (letting you spawn enemies off screen).
Make it so you can load sprites instead of just the built in one
Make errors not require the console to see.
Put it on itch.io.

Other:

If a subroutine shares a variable name with a global, it uses the global variable instead of the local one. Fix this, or at least make an error if you try it.
Make it so variables need to be explicitly declared, so typos will be more obvious
Collisions
Make it so enemies can have multiple overlapping sprites spinning at different rates
Separate compound_shader into shader include files
Elif statements
Make it so you can print messages on the screen. Players won't want to use the console, and ideally it could allow dialogue.
Circular screen

Allowing fields in scripts:

This is something I want to add later. It would be nice to use it to modify properties of the bullet rather than built in variables or functions. ChatGPT tells me I can use Object.Set() when setting properties on the left side, and override Object._Set() in EntityProperties (which I will make for this purpose) so it can tell me when a property has been updated and make sure the game reflects that.

Now that I have a good way going forward, I'm going to just make a ScriptContext.setProperties() function. If I add more properties to set it can get unwieldly, but if I have better method in the future, I don't have to worry yet.
